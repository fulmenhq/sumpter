# 13 FixtureDocument v1

Hermetic extract of synthetic `FixtureDocument schemaVersion="1"` records.
Only `Record` rows with `enabled="1"` (or `true`) are selected.

Run:

```bash
examples/scripts/run-case.sh examples/cases/13-fixture-document
```
