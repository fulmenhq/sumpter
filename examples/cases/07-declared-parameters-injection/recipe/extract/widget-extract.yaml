record_type: widget_parameter_order
match_selectors:
  - xpath: //Order
field_mappings:
  - output_field: order_id
    xpath: "@id"
    type: string
output_schema:
  type: object
  properties:
    order_id:
      type: string
    tenant_id:
      type: string
    region_id:
      type: string
  required: [order_id, tenant_id, region_id]
